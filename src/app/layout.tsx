import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Shuangning Wei | Software Engineer",
  description: "Personal portfolio of Shuangning Wei - Software Engineer with a unique background in Architecture. Showcasing projects in full-stack development, microservices, and data analytics.",
  keywords: ["Software Engineer", "Full Stack Developer", "React", "Next.js", "Python", "Java", "Portfolio"],
  authors: [{ name: "Shuangning Wei" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased tech-grid-bg min-h-screen flex flex-col`}
      >
        <Navbar />
        <main className="flex-1 pt-20">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
